{% extends 'dashboard/dashboard.html.twig' %}

{% block title %}Anzeigen{% endblock %}

{% block content %}
    <h1>Anzeigen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ site_config.id }}</td>
            </tr>
            <tr>
                <th>Configkey</th>
                <td>{{ site_config.configkey }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ site_config.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('site_config_index') }}">back to list</a>

    <a href="{{ path('site_config_edit', {'id': site_config.id}) }}">edit</a>

    {{ include('site_config/_delete_form.html.twig') }}
{% endblock %}
